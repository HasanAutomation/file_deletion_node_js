const fs = require('fs/promises');
async function countDown(number) {
  // console.log(number);
  // let newNumber = number - 1;

  // if (newNumber > 0) countDown(newNumber);
  try {
    console.log('Hasan\n\n\n\n');
    console.log('Ali');
  } catch (err) {
    console.log('err', err.message);
  }
}

countDown(10);
